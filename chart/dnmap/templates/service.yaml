apiVersion: v1
kind: Service
metadata:
  name: {{ include "dnmap.fullname" . }}
  labels:
    {{- include "dnmap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dnmap.selectorLabels" . | nindent 4 }}

